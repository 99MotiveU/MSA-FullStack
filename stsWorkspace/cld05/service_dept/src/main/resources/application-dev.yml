server:
  port: 7001

spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:./db/data
    username: h2

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update

logging:
  level:
    web: debug
    '[com.sts]': debug